///
/// HybridNitroMaskComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridNitroMaskComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitromask::views {

  extern const char HybridNitroMaskComponentName[] = "NitroMask";

  HybridNitroMaskProps::HybridNitroMaskProps(const react::PropsParserContext& context,
                                             const HybridNitroMaskProps& sourceProps,
                                             const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    mask([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("mask", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.mask;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.mask);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMask.mask: ") + exc.what());
      }
    }()),
    value([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("value", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.value;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.value);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMask.value: ") + exc.what());
      }
    }()),
    onChangeText([&]() -> CachedProp<std::optional<std::function<void(const std::string& /* maskedValue */, const std::string& /* rawValue */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onChangeText", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onChangeText;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::string& /* maskedValue */, const std::string& /* rawValue */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onChangeText);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMask.onChangeText: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroMaskSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroMaskSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMask.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridNitroMaskProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("mask"): return true;
      case hashString("value"): return true;
      case hashString("onChangeText"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroMaskComponentDescriptor::HybridNitroMaskComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridNitroMaskComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                     const std::shared_ptr<const react::Props>& props,
                                                                                     react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroMaskShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroMaskComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridNitroMaskShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridNitroMaskProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridNitroMaskProps>& props = std::const_pointer_cast<HybridNitroMaskProps>(constProps);
    HybridNitroMaskState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitromask::views
